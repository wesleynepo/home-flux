apiVersion: v1
kind: Service
metadata:
  name: myspeed 
  namespace: default
labels:
    app: myspeed 
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
      protocol: TCP
  selector:
    app: myspeed 
